{
    "name": "ticketfile",
    "displayName": "Ticket Explorer",
    "description": "Manage plain-text tickets from Visual Studio Code.",
    "version": "0.0.1",
    "publisher": "mackron",
    "engines": {
        "vscode": "^1.74.0"
    },
    "categories": [
        "Other"
    ],
    "main": "./extension.js",
    "contributes": {
        "viewsContainers": {
            "activitybar": [
                {
                    "id": "ticketfile",
                    "title": "Ticket Explorer",
                    "icon": "resources/ticket.svg"
                }
            ]
        },
        "views": {
            "ticketfile": [
                {
                    "id": "ticketfile.tickets",
                    "name": "Tickets"
                }
            ]
        },
        "commands": [
            {
                "command": "ticketfile.refresh",
                "title": "Refresh Tickets",
                "icon": "$(refresh)"
            },
            {
                "command": "ticketfile.createTicket",
                "title": "Create Ticket",
                "icon": "$(add)"
            },
            {
                "command": "ticketfile.openTicket",
                "title": "Open Ticket",
                "icon": "$(go-to-file)"
            },
            {
                "command": "ticketfile.closeTicket",
                "title": "Close Ticket",
                "icon": "$(issue-closed)"
            },
            {
                "command": "ticketfile.reopenTicket",
                "title": "Reopen Ticket",
                "icon": "$(history)"
            }
        ],
        "menus": {
            "view/title": [
                {
                    "command": "ticketfile.createTicket",
                    "when": "view == ticketfile.tickets",
                    "group": "navigation@1"
                },
                {
                    "command": "ticketfile.refresh",
                    "when": "view == ticketfile.tickets",
                    "group": "navigation@2"
                }
            ],
            "view/item/context": [
                {
                    "command": "ticketfile.openTicket",
                    "when": "view == ticketfile.tickets && viewItem =~ /Ticket$/",
                    "group": "inline@0"
                },
                {
                    "command": "ticketfile.closeTicket",
                    "when": "view == ticketfile.tickets && viewItem == openTicket",
                    "group": "inline@1"
                },
                {
                    "command": "ticketfile.reopenTicket",
                    "when": "view == ticketfile.tickets && viewItem == closedTicket",
                    "group": "inline@1"
                },
                {
                    "command": "ticketfile.openTicket",
                    "when": "view == ticketfile.tickets && viewItem =~ /Ticket$/",
                    "group": "navigation@1"
                },
                {
                    "command": "ticketfile.closeTicket",
                    "when": "view == ticketfile.tickets && viewItem == openTicket",
                    "group": "1_modification@1"
                },
                {
                    "command": "ticketfile.reopenTicket",
                    "when": "view == ticketfile.tickets && viewItem == closedTicket",
                    "group": "1_modification@1"
                }
            ]
        },
        "configuration": {
            "title": "Ticket Explorer",
            "properties": {
                "ticketfile.ticketsFolder": {
                    "type": "string",
                    "default": "tickets",
                    "description": "Path to the tickets folder, relative to the first folder in the workspace."
                }
            }
        }
    }
}
